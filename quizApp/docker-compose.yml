version: "3.8"

services:
  quizapp:
    build: .
    container_name: quizzie-app
    ports:
      - "8080:8080"
    environment:
      - ENV=production
      - QUESTION_PATH=https://github.com/stclaird/questionPack/releases/download/v1.0.2/questionPack-latest.zip
    volumes:
      - ./api/questionPack:/project/api/questionPack:ro
    restart: unless-stopped
    networks:
      - quizzie-network

networks:
  quizzie-network:
    driver: bridge
