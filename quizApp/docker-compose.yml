version: "3.8"

services:
  quizapp:
    build: .
    container_name: quizzie-app
    ports:
      - "8080:8080"
    environment:
      - ENV=production
      - QUESTION_PATH=https://raw.githubusercontent.com/stclaird/questions/refs/heads/main/gcp_gce_loadbalancer.json
    volumes:
      - ./api/questionPack:/project/api/questionPack:ro
    restart: unless-stopped
    networks:
      - quizzie-network

networks:
  quizzie-network:
    driver: bridge
